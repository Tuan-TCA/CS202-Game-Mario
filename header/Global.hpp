#pragma once
#include <iostream>
#include <vector>
#include <string>
using namespace std;

enum class SceneMode {
    GAME,
    MENU,
    PAUSE,
    WIN
};

const int screenWidth = 1600;
const int screenHeight = 800;
